// Config plugin placeholder for the RoomCaptureView (RoomPlan) native module.
// ceiling: identity no-op until the Swift module lands; reserves the app.json slot
// so nobody has to edit app.json again to add it.
module.exports = function withRoomCapture(config) {
  return config;
};

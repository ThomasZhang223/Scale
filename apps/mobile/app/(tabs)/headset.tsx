import { Text } from "react-native";

// Placeholder — Panel B owns this screen. Headset handoff and the stub-layer
// health row (see .claude/workstreams/thomas.md, "Start here") go here.
export default function HeadsetScreen() {
  return <Text>Headset</Text>;
}

import { Text } from "react-native";

// Placeholder — Panel B owns this screen. Capture hub (links into
// app/capture/room and app/capture/object) goes here.
export default function CaptureHubScreen() {
  return <Text>Capture</Text>;
}

# src/room

Rebuild the room from `RoomCapture v1` primitives: walls as boxes, openings as gaps, unscanned
furniture as per-category proxies. See `TRANSFORMS.md` in this directory before writing any
transform code.

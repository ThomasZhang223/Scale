import React from 'react';
import ReactDOM from 'react-dom/client';

// Scaffold entry point. Room/object/fit rendering and the XR canvas are not implemented
// yet — see src/room, src/objects, src/fit, src/ui.
function App() {
  return <div style={{ fontFamily: 'sans-serif', padding: '1rem' }}>apps/xr scaffold — nothing implemented yet</div>;
}

ReactDOM.createRoot(document.getElementById('root')).render(<App />);

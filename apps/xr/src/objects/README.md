# src/objects

GLB loading and placement. Loads `Object v1.glbUrl` by URL and places it per `Placement v1`.
Never rescales a loaded GLB — see the mesh normalisation contract in `.claude/contracts.md`.

{
  "name": "room-scan-headset",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "quest": "adb reverse tcp:5173 tcp:5173 && vite",
    "build": "tsc --noEmit && vite build",
    "test": "node --test src/*.test.ts",
    "deploy": "npm run build && wrangler deploy",
    "dev:cf": "npm run build && wrangler dev --port 8790"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.20.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "wrangler": "^4.135.0"
  }
}

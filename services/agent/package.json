{
  "name": "designer-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8789",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/pipeline/*.test.ts"
  },
  "dependencies": {
    "agents": "^0.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "@types/node": "^26.6.2",
    "typescript": "^5.6.0",
    "wrangler": "^4.135.0"
  }
}

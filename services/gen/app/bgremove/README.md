# bgremove

Background removal, run before generation on both the phone-scan and catalog input paths. Not
implemented — see `../../README.md` for the cut list (this is cut #2: shoot against a clean
surface instead if there's no time).

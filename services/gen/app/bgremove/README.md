# Background removal

The prepared SF3D package uses its existing rembg/u2net preprocessing path. A
second service and standalone `/bgremove` stub are cut from hackathon scope.
This path still needs a real SF3D run; it is not validated by local binder tests.
Capture against a clean background. See [SF3D](../../deploy/sf3d/README.md).

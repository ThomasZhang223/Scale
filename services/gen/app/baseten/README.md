# baseten

The Baseten client and both image-to-3D tiers behind one `tier: "live" | "quality"` parameter.
`live` is Stable Fast 3D, `quality` is TRELLIS 2 or Hunyuan3D Pro. Not implemented — see
`../../README.md`.

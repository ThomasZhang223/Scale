# embedding

CLIP ViT-L/14 image embeddings (768-dim, cosine metric), written into the `objects-v1` Vectorize
index alongside a caption and a colour palette, when an object's `state` flips to `"ready"`. Not
implemented — see `.claude/contracts.md` for the Vectorize index shape.
